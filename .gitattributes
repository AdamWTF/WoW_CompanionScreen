# Line endings: normalised to LF in the repository, checked out as CRLF. Pinning both ends means a
# tool run from a POSIX shell cannot rewrite the whole tree's endings as a side effect of editing a
# handful of files. text=auto still detects binaries and leaves them alone.
* text=auto eol=crlf
