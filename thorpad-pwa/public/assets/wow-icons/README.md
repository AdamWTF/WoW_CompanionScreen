# WoW icon assets

Place legally sourced WoW 3.3.5a icon files in this directory as lowercase WebP files, using the final component of the bridge icon path. For example, `Interface\\Icons\\Spell_Fire_FlameBolt` resolves to `spell_fire_flamebolt.webp`.

ThorPad falls back to a packaged neutral action icon if a requested asset is unavailable.
